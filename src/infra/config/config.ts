export const config = {
    key: "my-secret-key",

    db: {
        user: "postgres",
        password: "root",
        host: "localhost",
        port: 5432,
        database: "database_app",
    }
}